# Render deployment configuration for fair-foods server
# This ensures both client and server are built during deployment
services:
  - type: web
    name: fair-foods-server
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
